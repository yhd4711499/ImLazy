<UserControl x:Class="ImLazy.Addins.ConditionsConfigAddinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             LocalizationScope.ResourceManager="{ResourceManager AssemblyName='ImLazy',BaseName='ImLazy.Properties.Resources'}">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock 
        		HorizontalAlignment="Left" 
        		TextWrapping="Wrap" 
        		Text="{Loc When}" 
        		VerticalAlignment="Center"/>
            <ComboBox x:Name="CmbMode" VerticalAlignment="Center" Margin="5,0" DisplayMemberPath="Key" SelectedValuePath="Value"/>
            <TextBlock 
        		HorizontalAlignment="Left" 
        		TextWrapping="Wrap" 
        		Text="{Loc ConditionMatches}" 
        		VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
