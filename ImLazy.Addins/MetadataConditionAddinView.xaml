<UserControl x:Class="ImLazy.Addins.MetadataConditionAddinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="PropertyItemTemplate">
            <TextBlock Text="{Binding CanonicalName}"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ComboBox x:Name="cmb_Properties" SelectionChanged="cmb_Properties_SelectionChanged" SelectedIndex="0"/>
        <ComboBox x:Name="cmb_AvailSymbols" Grid.Column="1"/>
        <ContentPresenter x:Name="content_param" Grid.Column="2"/>
    </Grid>
</UserControl>
