<UserControl x:Class="ImLazy.Addins.Conditions.FileTypeConditionAddinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:ImLazy.Addins.Properties"
             LocalizationScope.ResourceManager="{ResourceManager p:Resources}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Loc Is}" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <ComboBox 
            x:Name="cmb_FileTypes"
            SelectedIndex="0"
            Margin="0,0,8,0" 
            VerticalAlignment="Center"
            SelectedValuePath="Value"/>
	</StackPanel>
</UserControl>
