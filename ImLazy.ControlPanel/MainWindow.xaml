<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:views="clr-namespace:ImLazy.ControlPanel.Views"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:Custom="http://www.galasoft.ch/mvvmlight" x:Class="ImLazy.ControlPanel.MainWindow"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="RuleItemTempalte">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}"/>
                <Button Content="..." HorizontalAlignment="Left" Width="15" Grid.Column="1">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding EditCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Window.DataContext>
        <Binding Mode="OneWay" Path="Main" Source="{StaticResource Locator}"/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <views:ServiceControlView/>
        </Grid>
        <Grid Grid.Row="1" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            </Grid>
            <ListBox x:Name="listBox" ItemsSource="{Binding Folders}" Margin="0,0,0,24"/>
            <Button Content="+" Width="75" VerticalAlignment="Bottom" HorizontalAlignment="Left" Command="{Binding AddFolderCommand}"/>
            <ListBox x:Name="lst_Rules" ItemsSource="{Binding Rules}" ItemTemplate="{DynamicResource RuleItemTempalte}" HorizontalContentAlignment="Stretch" Grid.Column="1" Margin="0,0,0,24" DataContext="{Binding SelectedItem, ElementName=listBox}"/>
            <Button Content="+" Width="75" VerticalAlignment="Bottom" HorizontalAlignment="Left" Command="{Binding AddRuleCommand}" Grid.Column="1" DataContext="{Binding DataContext, ElementName=lst_Rules}"/>
        </Grid>
    </Grid>
</Window>
