<UserControl x:Class="ImLazy.ControlPanel.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Image
                Height="192" Width="192"
                Margin="{StaticResource DefaultMargin}"/>
            <TextBlock
                HorizontalAlignment="Center"
                Margin="{StaticResource DefaultMargin}" FontSize="36">ImLazy</TextBlock>
            <TextBlock
                Margin="{StaticResource DefaultMargin}" HorizontalAlignment="Center">
                Version: 0.0 (build 0001)
            </TextBlock>
            <TextBlock
                Margin="{StaticResource DefaultMargin}" HorizontalAlignment="Center">
                Author: Ornithopter
            </TextBlock>
            <Button 
                Margin="{StaticResource DefaultMargin}"
                Width="85" IsEnabled="False">Visit website</Button>
        </StackPanel>
    </Grid>
</UserControl>
